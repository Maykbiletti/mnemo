# Example Pack

Template for a private Mnemo pack.

Copy this directory, rename it, and edit `pack.js` with your own values. Do not
commit real private data into the public repository.

```bash
cp -r packs/example-pack packs/my-private-pack
node packages/core/bin/mnemo.js pack apply my-private-pack
```
